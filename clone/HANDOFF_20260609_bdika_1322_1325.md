# Bdikot #1322–#1325 — handoff (2026-06-09, plonter_3_bot worker, BUILD-ONLY)

Source of truth: live `https://iseemath.co/plonter/clone/vocab.html`. Local
`clone/vocab.html` SHA matched live (`6d3b13e5…`) before edits — no stale fork.
All edits are in `clone/vocab.html`. NOT deployed (manager handles exposure
window + `?v=` cache-bump on deploy).

## #1322 (אוצר-מילים) — diacritic corrections, category 'חפיפה - מושכים בחות׳ים' — DONE
9 corrections applied to embedded category data (lines ~3311–3331):
- اِعْتَرَضَ هُ → **إِعْتَرَضَ هُ** (initial hamza)
- اِقْتَحَمَ هُ → **إِقْتَحَمَ هُ**
- اِمْتَلَكَ هُ → **إِمْتَلَكَ هُ**
- اِسْتَوْلَى عَلَى → **إِسْتَوْلَى عَلَى**
- عَرَبِيَّة → **عَرَبَة**
- قَوَارِب → **قَوَارِبُ** (final damma, no-tanwin)
- بِاسْم → **بِٱسْم** (wasla)
- الصَّوَارِيخ → **ٱلصَّوَارِيخ** (wasla on ال)
- صَوْب → **صَوْبَ** (final fatha)

## #1324 (אוצר-מילים) — photo content, group חפיפה / category חג החירות — DONE
The category 'חפיפה - חג החירות' ALREADY contained the 7 word-pairs from the
photo (added a prior session). Verified each against the image
(`photo_tmp_plonter_3_bot_0.jpg`). Only diff: photo shows **دُونَ شَكّ** (fatha on
ن); local had `دُون شَكّ`. Fixed → `دُونَ شَكّ`. No new rows needed.

## #1323 (אוצר-מילים) — restore note buttons / shortcuts / memory — DONE (RESTORED, not invented)
The audit-game note feature (owner-gated dev tool, used with the fixed buttons
חסר ריבוי / חסר מושא / חסר עהפ בעתיד) existed in the **legacy root**
`vocab.html` but was never carried into the clone — that is the "removed"
feature. Ported the complete block from root `vocab.html` (lines 10906–11102)
into `clone/vocab.html`:
- Recent-note **memory** (`plonter_audit_recent_notes_v1_<user>`, limit 12) —
  `_auditRememberRecentNote` / `_auditLoadRecentNotes`; `auditSaveNote` now
  remembers each note.
- **Suggestions** dropdown over the note textarea (`#audit-note-suggestions`),
  filtered as you type, click to apply (`_auditRenderNoteSuggestions` /
  `_auditApplyRecentNote`) — this is the "previously typed note suggested later"
  and the custom note-suggestion buttons Amitai asked for.
- Note toolbar: ע↔ع Hebrew↔Arabic convert (`_auditConvertNoteSelection`),
  reversed-kasra / reversed-damma inserts, and a full **niqqud palette**
  (`_auditNiqqudPaletteHtml` / `_auditApplyNiqqud`).
- Textarea rewired: `onfocus`/`oninput` render suggestions, `onblur` saves+hides.
All dependencies are either inside the ported block or `typeof`-guarded
(`DetailsPanel`, `transliterate`, `PlonterAuth`); clone already has
`_currentVocabOwner` / `_auditEscape` / `_loadAuditState` / `_auditWordKey` /
`_saveAuditState`.

## #1325 (אפליקציות) — "מושכים בחות׳ים" not visible in "יישומים" — SAME ROOT CAUSE AS #80
Not a clone bug. The clone groups 'חפיפה - מושכים בחות׳ים' correctly under the
'ישומים' group (bucket logic line ~7792; group render line ~8133). The reason
Amitai doesn't see it: his installed Android app loads the **legacy**
`/plonter/vocab.html`, which has 0 occurrences of "מושכים בחות" and no 'ישומים'
group. Fixed by the **v1.12 APK** (task #80) that repoints the app to
`/plonter/clone/`. No additional code change here. He must install v1.12.

## Verification
- node `--check` on all 7 inline `<script>` blocks of clone/vocab.html → ALL OK.
- Headless Chromium load of clone/vocab.html: page inits, NO JS-module 404s
  (only the backend `/plonter/api/...` 404 expected on a static server).
- In-browser probe: all 11 ported functions are `typeof function`; note-memory
  roundtrip works (remembered + recalled a note); niqqud palette HTML generates.
- Photo cross-check for #1324; live marker comparison legacy-vs-clone for #1325.

## Remaining / manager next steps
- Exposure-window deploy of clone/vocab.html (bump `?v=` per CLAUDE.md). Worker
  did NOT deploy.
- Device/owner QA of the audit-note UI (owner-gated to Amitai; couldn't enter
  the gated game headlessly — verified at function + memory + palette level).
- Install/distribute v1.12 APK so #1325 (and the #80 staleness) resolve on device.
